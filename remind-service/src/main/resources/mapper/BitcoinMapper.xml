<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper
	namespace="com.remind.persistence.mapper.BitcoinMapper">

	<select id="randOne"
		resultType="com.remind.persistence.domain.Bitcoin">
		SELECT * FROM bitcoin ORDER BY RAND() LIMIT 1
	</select>

</mapper>